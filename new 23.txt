@isTest
private class WarehouseSyncTest 
{
	@isTest static void checkTheSch() 
	{
		// Implement test code
		Test.startTest();
       	WarehouseSyncSchedule syncMe = new WarehouseSyncSchedule();
       	Datetime dt = Datetime.now().addMinutes(3);
        String CRON_EXP = '0 '+ dt.minute() + ' * ' + dt.day() + ' ' + dt.month() + ' ? ' + dt.year();
        String jobID = System.schedule('RUN_NOW', CRON_EXP, syncMe);
        Test.stopTest();
        CronTrigger ct = [SELECT State FROM CronTrigger WHERE Id = :jobID];
        System.assertEquals('WAITING', (ct.State));
	}
}


<urn:sessionId>00D6F000002RK7X!AQMAQPbOGJ7.h7wuXvRxl7Z6ry2tTyhl.1ATmHZ74eEaaLqLwnLXKj4TtvX3RRyKWmq28fjILDtLuFeLt12Y4lRhnqttEUAC</urn:sessionId>

SELECT SKU__c, ID FROM Product2 WHERE CPQ_Discount_Class__c = 'Perpetual' AND CPQ_PDH_Product_Family__c = 'Pivotal'
x`

Perpetual Pivotal Prods

IoT class as well
